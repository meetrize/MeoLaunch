{
  "name": "meolaunch-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name=meolaunch --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
